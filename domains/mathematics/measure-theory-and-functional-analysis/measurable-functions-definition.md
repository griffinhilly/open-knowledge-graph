---
id: measurable-functions-definition
title: 'Measurable Functions: Definition and Properties'
domain: mathematics
course: measure-theory-and-functional-analysis
prerequisites:
- id: sigma-algebras-and-measurable-sets
  type: hard
- id: open-sets-topology
  type: soft
builds-toward:
- simple-functions-approximation
- lebesgue-integral-simple-functions
tags:
- measure-theory
- measurable-functions
stage: advanced
status: draft
---

# Measurable Functions: Definition and Properties

## Core Idea
A function f: X → ℝ is measurable if the preimage of every Borel set in ℝ is a measurable set in X (i.e., f⁻¹(B) ∈ ℱ for all B ∈ ℬ(ℝ)). Measurable functions are precisely those we can integrate.

## How It's Best Learned
Start by showing continuous functions are measurable (preimages of open sets are open). Build to indicator functions of measurable sets.

## Common Misconceptions
Measurability differs from continuity; discontinuous functions can be measurable. You only need f⁻¹(open interval) measurable, not f⁻¹(single point).

## Questions

```yaml
- question: "A student claims that a function f: ℝ → ℝ cannot be measurable unless it is continuous, reasoning that 'discontinuous functions cannot respect the measure structure.' This claim is:"
  type: multiple-choice
  options:
    - "Correct — measurability requires continuity so that preimages of open sets are open"
    - "Incorrect — measurability only requires preimages of Borel sets to be measurable; discontinuous functions like indicator functions of measurable sets are fully measurable"
    - "Incorrect — measurability is actually a strictly stronger condition than continuity"
    - "Correct for functions on ℝ but incorrect for abstract measurable spaces"
  answer: 1
  explanation: "Measurability and continuity are different conditions. Continuity requires f⁻¹(open set) = open set. Measurability only requires f⁻¹(Borel set) ∈ ℱ. Since open sets are Borel, continuous functions are automatically measurable — but the converse fails. The indicator function 1_E of any measurable set E is measurable: f⁻¹ of any Borel set is either E, Eᶜ, ∅, or X — all measurable — yet 1_E is wildly discontinuous. Measurability is the weaker, more permissive condition."

- question: "To verify that f: X → ℝ is measurable with respect to σ-algebra ℱ, the minimal condition to check is:"
  type: multiple-choice
  options:
    - "f⁻¹({r}) ∈ ℱ for every single point r ∈ ℝ"
    - "f⁻¹(B) ∈ ℱ for every Borel set B ⊆ ℝ (the full definition)"
    - "f⁻¹(a, b) ∈ ℱ for every open interval (a, b) ⊆ ℝ"
    - "f is continuous except at countably many points"
  answer: 2
  explanation: "Checking open intervals is sufficient because the Borel σ-algebra is generated by open intervals, and preimages preserve all set operations: f⁻¹(Bᶜ) = (f⁻¹(B))ᶜ and f⁻¹(∪Bᵢ) = ∪f⁻¹(Bᵢ). So if f⁻¹(a,b) ∈ ℱ for every open interval, then f⁻¹ sends all complements and countable unions of open intervals — hence all Borel sets — to measurable sets. Option A (single points) is insufficient: even if every fiber f⁻¹({r}) is measurable, that does not force preimages of intervals to be measurable."

- question: "A pointwise limit of measurable functions is measurable, whereas a pointwise limit of continuous functions need not be continuous — and this is one reason measurability, not continuity, is the correct condition for Lebesgue integration."
  type: true-false
  answer: true
  explanation: "Lebesgue integration is built on approximating functions by simple functions (finite linear combinations of indicator functions), and taking limits. For this to work, the class of integrable functions must be closed under pointwise limits. Measurable functions have this property; continuous functions do not. (The pointwise limit of continuous functions is a Baire class 1 function, which may be discontinuous everywhere.) Measurability is precisely the condition broad enough to encompass the limits that arise naturally in integration theory."

- question: "To check that f: X → ℝ is measurable, it is necessary to verify that f⁻¹({r}) is in ℱ for every single point r ∈ ℝ; checking preimages of open intervals alone is insufficient."
  type: true-false
  answer: false
  explanation: "Checking preimages of open intervals is sufficient (and even equivalent to checking all Borel sets), because open intervals generate the Borel σ-algebra and preimages preserve the σ-algebra operations. Checking single points {r} is actually the weaker condition: it is possible to have all fibers f⁻¹({r}) measurable while f⁻¹(a,b) is not. The correct minimal check is open intervals (or equivalently, sets of the form (−∞, c) for all c ∈ ℝ)."

- question: "Why are preimages central to the definition of measurability, and what key property of preimages makes it sufficient to check only open intervals rather than all Borel sets?"
  type: short-answer
  answer: "Preimages are central because they preserve all set operations: f⁻¹(Bᶜ) = (f⁻¹(B))ᶜ and f⁻¹(∪Bᵢ) = ∪f⁻¹(Bᵢ). This means the collection {B ⊆ ℝ : f⁻¹(B) ∈ ℱ} is itself a σ-algebra. The Borel σ-algebra is generated by open intervals, so if f⁻¹(a,b) ∈ ℱ for every open interval, the closure properties of σ-algebras guarantee that f⁻¹(B) ∈ ℱ for every Borel set B — the full condition follows automatically. You only need to check generators."
  explanation: "This is a standard 'generator argument': to verify that a σ-algebra contains everything you need, it suffices to check a generating class, since σ-algebras are closed under complements and countable unions. Preimages propagate this closure property from the output space back to the input space."
```

## Explainer

You already know that a **σ-algebra** ℱ on a set X is a collection of "measurable" subsets — a structured family closed under complements and countable unions. Measurability of a function is defined by asking: does the function respect this structure? Specifically, f: X → ℝ is **measurable** if the preimage f⁻¹(B) = {x ∈ X : f(x) ∈ B} is in ℱ for every Borel set B in ℝ. The Borel sets are the σ-algebra generated by open intervals on ℝ, so this condition says: whenever you take a "natural" subset of the output space, the set of inputs that land there is a measurable set.

Why preimages? The key insight is that preimages preserve all set operations: f⁻¹(Bᶜ) = (f⁻¹(B))ᶜ and f⁻¹(∪Bᵢ) = ∪f⁻¹(Bᵢ). This means if you know f⁻¹ sends the generators of the Borel σ-algebra (open intervals) to measurable sets, then f⁻¹ sends all Borel sets to measurable sets — you get the full condition for free. This is why checking measurability is simpler than it looks: you only need to verify that f⁻¹(a, b) ∈ ℱ for all open intervals (a, b), or equivalently f⁻¹(−∞, c) ∈ ℱ for all c ∈ ℝ.

Continuous functions are automatically measurable. If X = ℝ with the Borel σ-algebra and f is continuous, then the preimage of every open set is open, and open sets are Borel sets. But measurability is strictly weaker than continuity — the **indicator function** of a measurable set E, defined as 1_E(x) = 1 if x ∈ E and 0 otherwise, is measurable even when E is a highly discontinuous set like the Cantor set or a measurable but non-open set. This is exactly why measure theory extends integration beyond Riemann's reach: we can integrate 1_E, and its integral is just the measure of E.

Measurable functions are the class precisely suited for the Lebesgue integral. The path forward is to integrate **simple functions** (finite linear combinations of indicator functions of measurable sets) first, then approximate general measurable functions by simple functions from below. This approximation works because measurable functions are closed under limits: a pointwise limit of measurable functions is measurable, provided the limit exists. Continuity has no such property — a limit of continuous functions need not be continuous. Measurability is the correct, more permissive condition that makes the Lebesgue theory coherent.
