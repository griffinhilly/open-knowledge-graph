---
id: cyclic-groups
title: Cyclic Groups
domain: mathematics
course: abstract-algebra
prerequisites:
- id: subgroups-subgroup-test
  type: hard
builds-toward:
- order-group-element
- classification-finite-abelian-groups
tags:
- cyclic
- generators
- powers
stage: advanced
status: draft
---

# Cyclic Groups

## Core Idea
A cyclic group is a group generated by a single element a, meaning every element can be written as a power of a. Cyclic groups are always abelian. Every cyclic group of order n is isomorphic to Z/nZ, and every cyclic group of infinite order is isomorphic to Z.

## Questions

```yaml
- question: "In ℤ/12ℤ, which of the following elements is a generator of the entire group?"
  type: multiple-choice
  options:
    - "4 — because it divides 12 evenly"
    - "6 — because it is exactly half of 12"
    - "5 — because gcd(5, 12) = 1"
    - "3 — because 3 is prime"
  answer: 2
  explanation: "An element k generates ℤ/nℤ if and only if gcd(k, n) = 1. Here n = 12: gcd(5, 12) = 1, so 5 is a generator. gcd(4, 12) = 4, gcd(6, 12) = 6, gcd(3, 12) = 3 — none equal 1, so 4, 6, and 3 each generate a proper subgroup. Being prime doesn't guarantee generation of the whole group; the condition is coprimality with the group order."

- question: "The Klein four-group V₄ = {e, a, b, ab}, where every non-identity element has order 2, is abelian. Why is it not cyclic?"
  type: multiple-choice
  options:
    - "V₄ is not cyclic because cyclic groups can only have prime order"
    - "V₄ is not cyclic because no single element generates all four elements — each non-identity element only generates a subgroup of order 2"
    - "V₄ is not cyclic because it has too many elements of the same order"
    - "V₄ is not cyclic because its generators must have prime order"
  answer: 1
  explanation: "A group is cyclic if and only if some element generates the entire group. In V₄, every non-identity element has order 2, so it generates only {e, itself} — a subgroup of order 2. No element produces all four elements, so V₄ is not cyclic. This shows that being abelian (which all cyclic groups are) does not imply being cyclic — the converse fails."

- question: "Every subgroup of a cyclic group is itself cyclic."
  type: true-false
  answer: true
  explanation: "This is a key structural theorem. If G = ⟨a⟩ is cyclic of order n, every subgroup consists of powers of a with a specific step size. The subgroups of ℤ/nℤ are exactly the sets generated by divisors of n: for each divisor d of n, there is exactly one subgroup of order d, namely ⟨d⟩. Each is cyclic. This clean structure has no analogue for general non-cyclic groups."

- question: "If a group G is abelian, then G must be cyclic."
  type: true-false
  answer: false
  explanation: "Being abelian is necessary but not sufficient for a group to be cyclic. Every cyclic group is abelian (since aᵐ · aⁿ = aᵐ⁺ⁿ = aⁿ · aᵐ), but the converse fails. The Klein four-group V₄ is a counterexample: abelian, but not cyclic because every non-identity element has order 2 and cannot generate all four elements."

- question: "In ℤ/6ℤ, explain why 2 is not a generator of the whole group, and identify what subgroup it does generate."
  type: short-answer
  answer: "gcd(2, 6) = 2 ≠ 1, so 2 does not generate all of ℤ/6ℤ. Adding 2 repeatedly: 2, 4, 6 ≡ 0 — we cycle back after 3 steps, never reaching 1, 3, or 5. The element 2 generates the subgroup {0, 2, 4}, which has order 3."
  explanation: "When gcd(k, n) = d > 1, the element k generates the subgroup of order n/d, consisting of all multiples of d. Here d = 2, so 2 generates the unique subgroup of order 3. This illustrates how the subgroup structure of ℤ/nℤ is determined entirely by divisors of n."
```

## Explainer

A **cyclic group** is the simplest kind of group: one generated entirely by repeatedly applying a single element to itself. If you call that element *a*, then the group consists of all "powers" of *a* — that is, a⁰ (the identity), a¹, a², a³, and so on, including negative powers (a⁻¹, a⁻², ...) when they exist. From your work with subgroups, you know that any subset closed under the group operation and inverses forms a subgroup. A cyclic group takes this further: a single element generates the *entire* group, not just a subgroup.

The two archetypes are the integers under addition (the infinite cyclic group, isomorphic to ℤ) and the integers mod n under addition (the finite cyclic group of order n, isomorphic to ℤ/nℤ). In ℤ, the element 1 generates everything: 1, 2 = 1+1, 3 = 1+1+1, and so on, together with −1, −2, ... In ℤ/nℤ, you have a clock with n positions: add 1 repeatedly and you cycle back to 0 after n steps. These are the only cyclic groups up to isomorphism — every cyclic group is one of these two types. Encountering a group and proving it is cyclic therefore pins down its structure completely.

Every cyclic group is **abelian** (commutative). This follows directly from the definition: since every element is a power of *a*, any two elements aᵐ and aⁿ commute — aᵐ · aⁿ = aᵐ⁺ⁿ = aⁿ · aᵐ. This is a strong structural constraint: if a group is non-abelian, it cannot be cyclic. But the converse fails — an abelian group need not be cyclic. For example, ℤ/2ℤ × ℤ/2ℤ (the Klein four-group) is abelian but not cyclic, because no single element generates all four elements.

One crucial tool is the **order** of an element: the smallest positive integer *k* such that aᵏ equals the identity. In ℤ/6ℤ, the element 2 has order 3 (since 2+2+2 = 6 ≡ 0), while 1 has order 6. An element generates the entire group ℤ/nℤ if and only if its order equals n, which happens exactly when gcd(element, n) = 1. Counting how many such generators exist leads naturally to Euler's totient function φ(n). Every subgroup of a cyclic group is itself cyclic — a clean structural theorem with no analogue for general groups.
