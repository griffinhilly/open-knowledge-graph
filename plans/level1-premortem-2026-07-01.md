# Level 1 Premortem — Capacity Hub on the Public Radial (2026-07-01)

**Purpose:** Premortem verdict on "Level 1" — rendering the 10 `kind:capacity` origin-layer nodes on the PUBLIC radial (porting the private `--with-origins` presentation). Run as the scoped §0.1 re-clear required by the Jun-30 ruling. Process: 3 Opus pessimists (bounce-rate / expert-reception / misreading-code lenses) → 2 Opus optimists (line-level verification) → 1 Opus risk assessor.

**Decision context:** Griffin decided "at least Level 0 [push commits — DONE Jul 1], probably Level 1" and delegated premortem scoping. Level 0 pushed `43284aaf7..fccaae4ba` to origin (Pages deploy triggered). NOT in Level 1 scope: capacity pages, sitemap/JSON-LD, headline counts, keystone eligibility.

## VERDICT: CONDITIONAL GO

Conditions (all buildable in one session):

1. **Section allowlist + relabel (scenarios A/G) — HARD GATE.** The private panel branch ships internal register verbatim: `visualize_radial.py:2224` hardcodes "private origin layer"; tooltips print raw `proto-formal`/`developmental origins` (label maps at 2171-2177 lack entries); the section loop (2225-2231) renders ALL body sections — including `## On the Name` (rename rationale) and `## Observable Signs` — into crawlable inline JSON. Fix: render only `Core Idea` + `Emerges Through` for capacities; replace panel meta; add label-map entries. **Acceptance check: grep the generated public HTML for "Observable Signs", "On the Name", "proto-formal", "private" → zero hits.**
2. **Hub de-emphasis (C) — HARD GATE.** Lone 4× white sun dead-center over-reads as single-origin, colliding with spec §1.3 ("a funnel, not a single-node pinch") and the lineage-not-continuity honesty cut. Fix: drop Discernment to ~2.5-3× (one number) so the picture reads as convergence.
3. **Legibility (B).** Hub disc (~8 world units) occludes inner-ring feeder discs (r=12, span 7-17) — one-number ring/size tune. Ancestry hop-cap 4 means the "roads lead to origins" payload doesn't fire by default — allow capacity-terminating expansion (terminate AT the capacity; don't traverse through, which would re-open scenario D).
4. **Two renames (independent, not gating):** only "Symbolic Function" and "Grading and Seriation" are genuine jargon; the other 8 titles are already plain ("Object Cohesion and Permanence", etc.). Rename those two to plain language; slugs unchanged.
5. **Click beacon (J) — campaign-gated, must exist before Jul-15.** Analytics are page-level only; a node-click/panel-open event must exist before the parent-push brings traffic, or the traffic yields no learning signal.

## Rulings on the open sub-decisions

- **§0.1 / Observable Signs public render: NO.** Already decided in origin-layer spec §0 condition 1 (authored-but-NOT-rendered); the code just doesn't honor it yet. Condition 1's allowlist enforces it. The §0.1 scoped premortem re-clear is hereby satisfied for Level 1 — Wonder-Card-style *rendered signs* remain gated for any future surface.
- **Rename sequencing: INDEPENDENT.** Renames don't gate the ship (8/10 titles already plain) but the two jargon cases are near-free — do them in the same session.

## Risk tiers

| Scenario | Tier |
|---|---|
| A Render-register leak (verified in code) | MITIGATE NOW — hard gate |
| B Cold-load illegibility / occlusion / hop-cap | MITIGATE NOW |
| C Visual single-origin claim (spec §1.3) | MITIGATE NOW — hard gate |
| G Observable Signs render | MITIGATE NOW (same fix as A) |
| J Unmeasurable success | MITIGATE NOW, campaign-gated (pre-Jul-15) |
| E Register mismatch (caregiver voice lives only in stripped sections — verified) | Resolved by A → MONITOR: post-ship spot-read all 10 rendered panels for second-person phrasing |
| F Expert takedown (hooks live in stripped sections) | Resolved by A → MONITOR: expert commentary if hub gets campaign exposure |
| I Parent-push channel misfire | MONITOR: early warning = any Jul-15 creative drafted with the radial as hero; centerpiece must be a deliberate decision, never a default |
| D Faculty-continuity screenshot | ACCEPT (4-hop cap structurally blocks; keep terminate-at-capacity constraint) |
| H Under-wired neighborhood (core-objects 5 topics; core-agents biology-dominant) | ACCEPT — iterable post-ship |

## Single biggest residual risk

**Uncontrolled screenshot semantics in the marketing channel (I × C).** If the radial becomes the Jul-15 campaign hero and a caption-free image of spines converging on a bright center circulates, the *picture* makes the single-origin/continuity claim the *copy* is bound never to make. Guard: any campaign use of the hub requires a baked-in caption and an explicit centerpiece decision.

## Fact corrections that emerged (don't let these propagate)

- **"Orphan capacities" claim is FALSE.** core-space and classification-sorting have empty prereq arrays in their own files, but 15+ / multiple public topics name them as prerequisites; radial edges are induced from topic→capacity listings, so both render with fan-out.
- **"Outdated Piagetian marker" claim is BACKWARDS.** core-objects' "acting surprised when an object seems to vanish" is the modern Baillargeon violation-of-expectation paradigm — a strength, not a liability.
- Real (minor, non-gating) expert nitpicks: ANS/parallel-individuation lumped as "two parts" of core-number; a Montessori three-period-lesson claim stated as acquisition-order fact in naming-symbol-reference; cross-file counting tension between core-number and symbolic-function. Document contested placements as explicit CHOICES per spec §0.

## Sharpest disagreements & resolution

- Pessimists framed the register leak (A) as the core reputational threat; optimists as a trivial ~10-line fix. Assessor: both right — cheap AND mandatory.
- Optimist split on rename gating resolved by spot-check: titles mostly plain already → INDEPENDENT.
