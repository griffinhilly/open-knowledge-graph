# Open Knowledge Graph Memory

## Status (Jun 30, 2026 — floor finished + Discernment rename + private viz + capacity-spine direction)
- **Floor extended 80 → 307/314 pre-formal topics** with a direct capacity prereq (7 unfloored on purpose, all reach a capacity transitively). `tools/wire_capacities.py` rewritten: per-topic regex over **TITLE+TAGS only** (course-name dropped — a course-name blanket match was the FP that stamped wrong capacities on 16 living-things topics) + curated `COURSE_DEFAULTS` + homonym guards + `reconcile_prereqs` (adds AND removes capacity edges to match target exactly; replaced the old add-only `insert_prereqs`). Review: `plans/origin-layer-wiring-review.md`.
- **Gotcha — plural/inflected regex boundaries:** `\bweav\b`/`\baddend\b`/`\bfact famil\b`/`\brespond\b` all failed on inflected forms; fixed by listing explicit forms (`weaving`, `addend|addends`, `fact family|fact families`, `responds|responding|response`). When matching child-topic titles, prefer explicit alternation over `\b`-anchored stems.
- **Connectivity invariant in `validate.py`**: every pre-formal `kind:topic` must reach a `kind:capacity` via ancestry (memoized DFS). 314/314 reach live; 0/314 if capacities ignored. This is the structural proof the origin layer is load-bearing.
- **Rename "Discrimination" → "Discernment"** (slug `discrimination-same-different` → `discernment-same-different`, file renamed). Surgical — only OUR capacity node + code labels; left unrelated legit uses (price-/item-/chord-discrimination) untouched; excluded the stale `.claude/worktrees/` snapshot.
- **Two PRIVATE viz** (capacities are internal-only; central hub, NO radial wedge — Griffin's call): `tools/visualize_origin_layer.py` → `output/origin-layer-map.html` (D3 force, 10 caps + 307 topics + 721 edges); `tools/visualize_radial.py --with-origins` → `output/radial-with-origins.html` (Discernment at r=0 center, 9 caps in inner ring; public radial unchanged). **Verified structurally only — browser was offline, NOT visually rendered. Eyeball before relying on them.** Griffin's "nodes should appear slightly differently" tweak still needs a direction.
- **CAPACITY-SPINE direction set (ideate dialectic, 5-2-3 Opus): Griffin chose TRUTH-FIRST.** Build the faculty-profile engine (B1 ancestry-flow propagation operator + B6 ~171-cell domain×thread prior + B9 keystone+altitude seeds; faculties OUTSIDE the graph = the B3 fork, NOT kind:faculty nodes) as a **PRIVATE truth-test** gated on 3 pre-registered validation gates (G1 face-validity, G2 factorization-recovers-the-hand-authored-basis, G3 faculty⊥domain discriminant) before any user surface. Synthesis: `plans/capacity-spine-synthesis-2026-06-30.md`. **Load-bearing honesty constraints**: (1) reproducibility ≠ validity — a wrong seed prior produces thousands of consistently-wrong-but-plausible profiles, so the gates are mandatory; (2) developmental "threads" are real at the BOTTOM (infant→early-academic) but METAPHOR at the top (infant→expert) — sell prerequisite LINEAGE, never faculty CONTINUITY; (3) faculty-strength inference from sparse fluency is statistically UNIDENTIFIED (collinearity + apophenia) — no "your weak spot is faculty X" claim; (4) "same faculty 30 years apart" contradicts the locked layered decision (it's the same THREAD, not faculty).
- **Track A (cheap, ships anytime, no gate):** rename the 10 capacities to parent-legible titles; one static "Cradle-to-Frontier" poster; an "orphan faculties" honesty post; codify anti-checklist + bounce-trap rules; instrument privacy-safe analytics. **§0.1 still binds** parent *rendered signs* (Wonder Cards) — needs a scoped premortem re-clear; renames + poster render no signs so they're free.

## Status (Jun 26, 2026 — origin layer pilot: `kind:capacity` substrate)
- **What & why**: added a developmental "origin layer" beneath the 314 disconnected pre-formal roots (which had no rooted substrate → path engine dead-ended). 10 `kind:capacity` nodes in a hidden `developmental-origins` meta-domain. Decided via probe→`--tradeoff`→`--premortem` (all artifacts in `plans/origin-layer-spec.md` + `precursor-primitives-probe-2026-06-26.md`).
- **The node-kind pattern (reusable)**: a new node lives IN the graph (so prereq/ancestry edges resolve) but is filtered OUT of every output surface with a one-line `data.get("kind")=="capacity"` guard. Guards added in: `generate_topic_pages.load_all_topics` (covers pages + keystone + JSON-LD via `pid in all_data`), `visualize_radial`, `visualize_hierarchy` (×2 loaders), `stats`, `generate_assessment`. KEPT in `generate_quiz_page._build_lightweight_graph` (graph.js — the structural win). Hidden meta-domain skipped in `visualize_domain_map --all` + `generate_sitemap` via a new `hidden: true` key in `_domain.yml` (passed through `parse_domain_yml`).
- **Fluency gotcha (premortem-caught, real)**: `fluency.js` forward-cap (`FORWARD_CAP_WEIGHT 0.9`) would cascade `0.9^depth` if a capacity were seeded to 100 as a hard prereq of a previously-root pre-formal topic. Fix = "assumed-known" implemented by (a) pruning `kind:capacity` from `computePathToGoal` and (b) skipping capacity prereqs in the cap loop — NOT by seeding score 100. Regression test: `tools/test_fluency_capacity.js` (run: `node tools/...`). The "blast radius is small" claim was about CENTRALITY only; fluency propagation was a separate, unmodeled surface.
- **Wiring**: deterministic `tools/wire_capacities.py` + a built-in anti-collapse gate (`--report`: ≥4 signatures, top ≤40%, `disc_only_frac ≤ 0.20` — relaxed Jun 30 from "every root ≥1 non-discernment cap" to allow legitimately auditory/tactile discernment-only roots). **Jun 30: floor finished at 307/314 (was 80); see the Jun-30 block above for the rewrite.** The gate measures signature DIVERSITY, not correctness — correctness still needs a human eyeball (the single biggest residual risk; keep substrate PRIVATE so wrong edges stay revisable, not crawler-published). *(Note: "discrimination" capacity renamed "discernment" Jun 30.)*
- **Schema additions**: `kind` (topic|capacity), status `reference`, stage `proto-formal` (added to `VALID_STAGES` + the `STAGE_RANK` audit list), `## Observable Signs` non-diagnostic lint in `validate.py` (rejects age-cutoff/delay language, requires the disclaimer). Invariant updated: "all validated" → "all `kind:topic` validated; capacities are `reference`."
- **ID hygiene**: capacity ids are collision-safe; `classification` was renamed `classification-sorting` (it's a substring of `classification-metrics` etc.). `dedup_analysis.STATUS_PRIORITY` got a `"reference"` key; `connect_leaves` got a `kind`-guard.
- **Identity decision (Griffin)**: keep the modest "map of what's taught and studied" identity; "everything learnable, grounded in innate capacities" is an aspirational North Star only, never a per-page/public claim.

## Status (Jun 12, 2026 — SEO sprint + domain go-live)
- **openknowledgegraph.com fully live**: HTTPS enforced, Cloudflare proxy + Full (strict), email-spoof lockdown, github.io 301s, Search Console verified + sitemap submitted Jun 12. Domain flip was 1 line (`SITE_BASE_URL`) + CNAME — centralization paid off same-session.
- **Strategy fork RESOLVED Jun 12 (Griffin): distribution track PRIMARY; parent-acquisition PARKED until ~mid-July** (re-entry for August back-to-school push). Key rationale: the parent-track pause was partly seasonal — promoting an educational resource to parents late in the school year felt futile; August school resumption is the natural window. Distribution sequence: ~~B3+A6~~ (BUILT Jun 12) → ~~keystone leaderboard~~ (BUILT Jun 12-13: Griffin's 3-principle scoring [0.6^hop decay / 6-hop horizon / 2× cross-domain / within-stage bands] in `generate_keystone_page.py`; edge-audit gate satisfied — 1,464 edges via 23 agents, 155 fixes + 5 merges applied, corpus now 15,285) → path engine (no-path-fallback gate) → B2 Show HN → .ics card. Pre-launch: README (repo has NONE), ~~analytics~~ (SHIPPED Jun 12, verify CF dashboard ~Jun 13), Node-24 action bumps. **og-card gotcha**: pill text truncates at `.chain-pill` max-width — current-topic is a glowing hue dot, never a (redundant, truncating) title pill; same pattern on the page's neighbor strip.
- **SEO infrastructure shipped** (was zero): shared `seo_meta_tags()`/`meta_description()`/`SITE_BASE_URL` in `tools/parse_topic.py`; all generators emit description/canonical/og; topic pages get `LearningResource` JSON-LD (`competencyRequired` = hard prereq titles); `tools/generate_sitemap.py` in CI. **Sitemap scope decision**: topic pages + 3 main pages + 19 domain maps; tag pages (20k) and `-questions` pages excluded as thin-content risk — revisit only with ranking data.
- **GitHub project-page caveat**: crawlers never read `output/robots.txt` (host-root only). Sitemap activation = manual Google Search Console submission (Griffin). robots.txt emitted anyway for a future custom-domain move.
- **Stage-card first-visit auto-show reversed** (Griffin + audit agreed it ambushed the first view). `okg-stage-card-dismissed` localStorage key is now write-only dead code.
- **Gotcha — phantom-node count inflation**: `load_graph(domain_filter=...)` in `visualize_hierarchy.py` appends phantom `domain:"external"` nodes so cross-domain prereq edges render. Any consumer that counts/sums those node lists overcounts (index hero said 16,951 vs 15,290 real). Fixed in index stats; check this if any new surface reports topic counts.
- **Audit + ideation artifact**: `plans/initial-view-and-usage-ideas-2026-06-10.md` (ranked Buckets A/B, sequencing, research evidence). Approved next: post-sprint `/dialectic-review --ideate` on it.

## Status (Jun 19, 2026 — Cosmos Institute / McCord funding intel)
- **Surfaced during an Agencyism deep dive on the Cosmos Institute (Brendan McCord).** Full note: `~/Projects/griffin/Agencyism/cosmos-mccord.md`.
- **Math Academy hostility worry = unfounded.** Searched McCord's site/Substack/podcasts/X/Crunchbase + every Cosmos grant cohort: **zero** financial, advisory, praise, or personal tie between McCord/Cosmos and Math Academy or its founders (Skycak/Roberts). And Math Academy is **bootstrapped with no outside investors** — no surface to invest in. The "McCord is a Math Academy investor who'd treat open-sourcing knowledge graphs with hostility" scenario has no factual basis.
- **Cosmos is a plausible (if unusual) OKG funder/ally.** McCord's one disclosed investment is **Prime Intellect** (decentralized *open-source* AI) — his money goes open/decentralized. Cosmos Grants explicitly fund open-source + "decentralized knowledge systems" (grantees incl. Sam Klein's AI-powered global digital library, Socratica's "AI librarian"; Koralus's open-source AI-tutor PyETR). Cosmos skews philosophy-of-AI *research* over shipping *infrastructure*, so OKG is an atypical-but-not-alien fit.
- **Framing implication (load-bearing): pitch OKG as autonomy-enabling self-directed inquiry, NOT "AI optimizes your learning."** McCord's Bildung / "preserve productive struggle" / anti-"autocomplete-for-life" frame is *skeptical* of algorithmic learning-path optimization (which cuts against Math Academy more than OKG) and *receptive* to open maps where the learner exercises choice. **This is the same wedge the Phase 12 dialectic already chose** ("OKG's wedge over Math Academy is breadth + self-direction, not sequenced mastery") — so OKG's existing competitive positioning IS the McCord-aligned pitch. If approaching Cosmos (grant/fellowship), lead with self-direction + open/decentralized + epistemic autonomy.
- *Unverifiable residue: angel investments not on Crunchbase; the "Cosmos Ventures" portfolio page wasn't accessible. Bootstrapped-Math-Academy fact moots the specific worry regardless.*

## Status (Apr 25, 2026 — parent-acquisition pivot; PARKED Jun 12 → re-entry ~mid-July)
- Full ideation + tradeoff verdicts at `plans/parent-acquisition-ideation.md` (committed Jun 11). Direction: B-first worksheet-OCR build, K-reframed positioning. First move on re-entry: the 2-day Twitter test gate (3 manual worksheet diagnoses → threads; ≥30 likes + ≥3 inquiry replies = build, <10 likes = `--premortem` on the wedge). Apr plan's May 23 kill-gate date passed unexecuted — externalize the July trigger, don't rely on session memory.
- **Load-bearing finding (all tracks): the zero-users base rate — feature-shipping produced zero users in 6 months; plans proposing more features need an explicit causal account of why this round differs.** Also durable: calibration ≠ topology verification (Rasch needs months of real-kid responses; never promise "calibrated diagnostic in weeks").

## Durable architecture facts
- **15,285 topics / 19 domains / 261 courses / 6 stages** (was 15,290; −5 from the Jun-12 keystone dup merges); radial shows 18 domains (practical-life-skills excluded). Live at `openknowledgegraph.com`; `SITE_BASE_URL` in `tools/parse_topic.py` is the single source of truth for all URLs.
- **Domain maps are primary navigation** (hierarchy views removed from CI). CI: validate → index → radial → topic pages → domain maps → assessment → quiz → sitemap → SEO validation.
- **Pre-push hook** (`git config core.hooksPath hooks`): cycle detection + CI-script tracking + quiz staleness + **dialectic-trailer gate (Jun 12)** — commits ≥5 non-md files or ≥200 non-md LoC need `dialectic-reviewed:`/`dialectic-skipped:` trailer.
- **Path engine primitives**: `computePathToGoal`/`computeLearningPath` in fluency.js (BFS + topo-sort); index learning-path section lazy-loads `js/graph.js` (3.5MB) only when goals exist. **Session 1 SHIPPED Jun 13** (radial): `centerOnNode(node,scale,screenDX)` (single camera-centring source), `withFullGraph(cb)` (lazy-loads graph.js once, gated on first reveal — keeps 3.5MB off cold path), `computeAncestry(G,startId)` (unbounded backward-BFS, cached per id), `revealAncestry(id,maxHops)` (hop-capped dim+overlay; default 4, Infinity=full), `pathNodeSet`/`pathEdges`+`drawPathOverlay()` dim render mode, `#pathBanner`. **Gotcha: graph.js compact `{d,c,p,s}` drops edge hard/soft type** — full-graph ancestry can't style hard vs soft (radial's own `data.edges` keeps type but is a filtered subset). Surfaces: A3 reveal, A2 search auto-reveal, B4 `?ancestry=<id>`. Session-2 A→B + no-path-fallback gate not yet built.

## 6-Stage Schema (Mar 22, 2026)
- **Stage criteria**: stage = where content is typically first encountered, NOT difficulty ceiling. "expert" = graduate/research; formal-systems = standard undergrad curriculum (not just proof). Referee case decisions → `guides/phase12-implementation-notes.md` appendix.

## Shared Parser (Apr 5, 2026)
- `tools/parse_topic.py` — canonical parsing module used by all CI tools (`parse_topic/parse_frontmatter/parse_sections/extract_questions`); since Jun 11 also home of `SITE_BASE_URL` + `seo_meta_tags()`/`meta_description()`.

## Phase 12 Implementation Detail (Apr 12, 2026) — MOVED
Full implementation notes, LoC ledgers, dialectic rulings detail, and reviewer findings for Phase 12A + 12B Cuts 5-7: **`guides/phase12-implementation-notes.md`**. Read it before resuming Phase 12 / fluency-engine / Sprout work. Live tripwires kept inline:
- **If Griffin-as-Persona-C complains his kindergarten topics render dim**: fix = asymmetric stage decay (`max(0, 1 - 0.4 * max(0, topic_stage - user_stage))`), NOT a fluency-engine bug hunt.
- **Future bulk-labeling QA**: eyeball random *agreements* (not just disagreements), run inter-agent reliability, expect flips proportional to label prevalence — full protocol in the guide.
- **Sprout hero-image retrofit** gated on first real Persona A tester (`HERO_IMAGE_RETROFIT` markers in `visualize_radial.py`).

## Phase 12A Follow-ups (not blocking 12B)

- **MC position-bias data fix**: rewrite `assessment-questions.json` with pre-shuffled options so the bias is gone at source. Runtime shuffle in `renderQuestionCard` is the load-bearing shield for now.
- **Course-level picker inside Deep Dive**: currently domain-only; Griffin mentioned wanting "domain/course" granularity.
- **Stage-inversion edge spot-check**: 2,325 edges have prereq staged more advanced than successor. The alpha gradient formula produces locally-inverted signals at those pairs. Low priority unless a visual glitch surfaces.
- **Asymmetric stage decay vs symmetric**: user-experience question, parked until real use.
- **Lightweight seed adaptivity**: rolling-window nudge after N questions if >75% or <25% correct. Deferred to Phase 12C per Griffin's call.

## Phase 12 Dialectic Rulings (Apr 11, 2026)

Non-default decisions from the 3-round multi-agent dialectic (Psychometrics / Math Academy / UX / Skeptic → Opus referee). Record so future sessions don't re-derive:

- **Graph-as-product wins over lesson-feed-as-product.** 3-of-4 lens convergence + Griffin's explicit virality framing. OKG's wedge over Math Academy is breadth + self-direction, not sequenced mastery. Rejection of lesson-feed applies to Personas B/C only — Sprout (12B) is a partial lesson-feed concession for Persona A, and that's OK as long as the conceptual boundary stays clean.
- **No new per-topic attributes.** Rejected: `general_knowledge_tier`, `lesson_minutes`, `diagnostic_value`. Rationale: 15,290 topics × new attributes = rot liabilities when topics get renamed/merged/restaged. Alternative: 19-row domain-level slider UI fed into cold-start prior. Cost 0 annotations.
- **Per-edge `strength` (hard/soft) is the ONE new annotation** that survived scrutiny. Per-edge (20-40K edges), not per-topic. Generated via one-time Haiku labeling pass with 200-edge sample QA first. Fixes the "all prereqs treated equally" data-quality bug masquerading as a model bug.
- **Pedagogy-typing at domain level** (`assessable` vs `reflective`) — referee called it the best idea in the dialectic not in the original brief. Reflective domains (literature, philosophy, art history, most of history, music appreciation) get mark-as-read + optional text field, NOT quizzes. Fixes "90% mastery on The Sublime in Wordsworth" absurdity. ~20 min of Griffin classification time on 19 `_domain.yml` files.
- **24Q seed is opt-in, not gated.** Virality preserves over calibration precision. First-time visitors see the radial immediately. Deeper calibration (19-row slider, 24Q seed) lives behind buttons for engaged users. *(Amended Jun 11, 2026: the stage-card first-visit auto-show was reversed — Level button is now the only entry.)*
- **FSRS rejected as retention mechanism.** Replaced by stale-topics query (>3 weeks since touch AND fluency ∈ [50,85]). Pure localStorage timestamp query, no scheduling engine. If long-term retention becomes the bottleneck, revisit — but not as Phase 12.
- **Rasch model is Phase 12C conditional only.** Stage heuristic (`domain_prior × stage_decay`) does the work in 12A. Rasch with θ∈ℝ^8 only if stage heuristic proves off by >1 stage in >20% of sampled users.
- **One component tree with conditional Sprout branch**, NOT three shell codebases. Three persona "presets" are thin preset bundles parameterizing one render path. Sprout is one `if (preset === 'sprout') return <SproutCard/>` conditional.

## Decisions
- **Format**: Markdown + YAML frontmatter (one file per topic)
- **License**: CC BY-SA 4.0 for content, MIT for tools
- **GitHub Pages**: Preferred deployment; generated HTML stays out of git
- **Status**: All topics now `validated` as of Mar 23
- **Domain ordering**: Math → Formal Sciences → Philosophy → CS → Engineering → Physics → Earth & Space → Chemistry → Biology → Health → Psychology → Social Sciences → Economics → Practical Life → History → Language → Literature → Arts → Music
- **Validation approach**: Haiku for volume, Sonnet for review, Opus for decisions
- **Stage audit principle**: stage based on actual file content, not whether simpler version exists for younger learners
- **localStorage keys**: `okg-fluency`, `okg-fluency-conf`, `okg-fluency-meta`, `okg-goals`, `okg-adjustments`

## Stage Tooling
- `tools/auto_promote_stages.py --apply` (promotes topics staged below their hard prereqs); `tools/audit_course_stages.py --fix`; course-stage audit also runs in `validate.py` full mode.

## Domain Map / Radial Layout — MOVED
- Layout architecture (tier layout, `COURSE_BRANCH_X`, sizing, centroid placement, radial branch integration) moved to `guides/visualization.md` (Jun 12). Companion tools: `tools/connect_leaves.py`, `tools/dedup_pairs.py` (dedup hazards → Gotchas).

## Cross-Domain Stage Calibration (Mar 29, 2026) — DONE, remainder deferred
- Root cause was **blanket course-level staging** (topics inherited course stage without individual evaluation); 1,446 topics restaged via 14-agent swarm. Per-domain counts in git history.
- **Still deferred**: ~60 missing expert topic-areas (inorganic chemistry entirely absent; quantum computing, cryptography, ML theory, algebraic topology, differential geometry, GR, QFT); `_domain.yml` course-stage reconciliation needs modal-stage (not minimum) logic; philosophy-of-science has ~10-12 dup pairs.

## Gotchas
- **GH Pages cert provisioning is one-shot**: adding a custom domain while DNS is Cloudflare-proxied makes the cert check fail silently and never retry (`https_certificate.state` stays null). Diagnose with `gh api repos/.../pages/health`; fix by removing + re-adding the domain (cert then issues in ~15 min). Sequence: gray-cloud DNS → set domain → cert → enforce HTTPS → orange-cloud + Full (strict).
- **Cross-tool imports**: `from parse_topic import ...` works from repo root because Python puts the script's own dir (`tools/`) on `sys.path[0]`.
- **Python f-string + JS templates**: `\'` inside f-string produces `'` not `\'`. Use `"'" +` concatenation instead.
- **Inline JSON size limit**: Browsers choke on 700KB+ single-line `<script>` data. Use `indent=1` for multi-line or external `.js` file.
- **Click-vs-drag in canvas views**: If two `mousemove` handlers share `dragStartX/Y`, track original position separately (`mouseDownX/Y`) for displacement detection.
- **Dedup agents introduce cycles**: Batch find-and-replace during dedup can create mutual prerequisite references. Always run cycle detection after dedup swarms. Also: broad text replace can corrupt IDs when delete_id is substring of keep_id (e.g., `prose-poetry` → `prose-poetry-hybrid-form` corrupted the keep file's own ID).
- **Dangling cross-domain refs**: 394 total across all domains (pre-existing, not from dedup). Mostly course names used as prereq IDs (`probability-and-statistics`, `macroeconomics`) instead of actual topic IDs. Doesn't affect domain maps (within-domain only).
- **Validation (Mar 26)**: 13,153 topics, 0 self-refs, 0 within-domain cycles. Graph is structurally clean after math dedup (91) + non-math dedup (149) = 240 pairs merged.
- **Dedup self-ref pattern**: When keep_file has builds-toward pointing to delete_id, the redirect creates a self-ref. The dedup script skips delete_file but not keep_file. Always run self-ref cleanup after dedup.
- **Dedup cycle pattern**: Soft prereqs added by `connect_leaves.py` can point backwards pedagogically. When dedup merges nodes, these create cycles. Fix: remove the backwards soft prereq.
- **T/F mechanical rewrite caveat**: Regex-based `always→typically` etc. can make false statements arguably true. Spot-check needed, especially `only→primarily`, `entirely→mostly`, `cannot→can rarely`. Also: "every"→"most" breaks plural agreement ("most day" instead of "most days") — fixed 23 instances via `tools/fix_grammar_rewrites.py`. Future batch rewrites should include grammar validation.
- **Canvas mouse/touch handler divergence**: When adding touch support, every mouse handler needs a touch equivalent. The radial touchend was missing domain label navigation that mouseup had. Consider refactoring to shared interaction functions.
- **Radial semantic zoom**: Course labels computed dynamically from visible nodes only — precomputed centroids break when courses span large radial distances (centroid is in the middle, zooming into one end loses the label). Must use viewport-filtered approach.
- **Radial viewport bounds**: Must divide by `camScale * viewScale`, not just `camScale`. viewScale = `Math.min(W,H) / 1200`.
- **Git worktrees**: Work well for parallel OKG development. Branches touched different file sets (tools/ vs domains/) so merges were clean. Remove worktrees before deleting branches. Kill HTTP servers before removing worktree directories.
- **Killing bash scripts doesn't kill `claude --print` children**: The orchestrator spawns `claude --print` as a subprocess. Killing the parent bash script (via `taskkill`) leaves the claude process running. This caused double-generation when a sequential batch was "killed" but its child completed. Use process groups or kill the claude PID directly.
- **Haiku MC option format bug**: Haiku agents frequently write MC options as `{0: "text"}` dicts instead of plain strings in YAML. Run a validation pass after any Haiku Q+E generation to catch these before pushing.
- **Haiku context limits for Q+E**: Haiku agents hit context limits at ~30-40 file edits and either stop or write placeholder content. Split courses into ≤30-file batches for Q+E agents.
- **Quiz state machine lives in `generate_quiz_page.py`, not `fluency.js`**: The 3-phase warmup/exploration/deep-dive orchestrator is embedded JS inside the Python generator (~250 lines). When reasoning about fluency engine LoC, don't conflate — `fluency.js` holds the Bayesian/propagation core, the Python generator holds the quiz orchestration.
- **Stage inversion edges interact with any stage-distance rendering**: 2,325 edges (~8%) have prereq staged more advanced than successor. Any new render logic using `stage_distance` (e.g., Phase 12 opacity field) will produce inverted local signals around those pairs. Spot-check on known-bad edges before shipping. Inversion list is derivable from `stats.py` output.
